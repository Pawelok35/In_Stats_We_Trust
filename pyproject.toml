[project]
name = "in-stats-we-trust"
version = "3.1.1"
requires-python = ">=3.10,<3.11"

[tool.black]
line-length = 100
target-version = ["py310"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

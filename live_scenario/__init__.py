"""Core helpers for the NFL Live Scenario module.

The package is introduced next to the legacy script. Production CLI/GUI behavior
still uses `scripts/live_quarter_scenario_matrix.py` until V2 passes comparison
tests.
"""

